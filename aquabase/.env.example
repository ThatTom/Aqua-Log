# Optional. If omitted, Docker startup uses admin@localhost and generates
# a stable password in /pb/pb_data/.aqua-log-admin-password.
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=change-this-before-running
